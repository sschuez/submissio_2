<div class="container">
	<div class="row">
		<div class="col-xs-12 col-sm-10 col-sm-offset-1 main-card">

			<%= simple_form_for @submissio do | f | %>
				<div class="form-group">
					<%= f.input :authority, label: 'Vergabestelle', error: 'Vergabestelle muss angegeben werden' %>
				</div>
				<%= f.button :submit, "Create Submissio", class: "btn btn-primary" %>

			<% end %>

		</div>
	</div>
</div>

